export class GameEspansion {
    
    constructor(private name: string, private totalPlayer: number, private description: string) {
        this.name = name;
        this.totalPlayer = totalPlayer;
        this.description = description;
    }
}